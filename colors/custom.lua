local base = require("colors.base")

local colorscheme = {
    tab_bar = {
        active_tab = {
            bg_color = base.green,
            fg_color = base.crust,
        },
    },
}

return colorscheme
